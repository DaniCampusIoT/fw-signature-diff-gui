# fw-signature-diff-gui
Modern Windows GUI tool to diff “original vs signed” firmware binaries, detect changed regions and automatically extract candidate signature blobs (RSA/ECDSA/EdDSA-friendly heuristics) with timestamped export folders.
